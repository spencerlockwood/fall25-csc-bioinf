
I used Claude pretty extensively in this assignment to help translate standard Python code into Codon-compatible versions. It AI helped me understand Codon's specific requirements, such as mandatory type annotations for empty collections, the prohibition of **kwargs, and proper import syntax (using from sys import argv instead of import sys). It also helped debug issues when the evaluation script wasn't working correctly, identifying that the MT FASTA files were missing their header lines. The AI provided complete implementations of all four alignment algorithms and created the evaluation script that runs all tests and formats the output.

The AI was valuable for understanding the differences between Python and Codon, such as strict separation of int and float types, and how to properly structure the project with test data files. I also used it to create the GitHub Actions workflow file based on an existing template from a previous week's assignment. Overall, the AI significantly accelerated development by providing working code examples and troubleshooting guidance, allowing me to focus on understanding the algorithms rather than wrestling with syntax issues.